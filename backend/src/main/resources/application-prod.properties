spring.application.name=qrsystem

# Database Settings - These will be provided by environment variables on Render
# Note: Render's DATABASE_URL usually starts with mysql://, we must prefix it with jdbc:
# and add SSL parameters for TiDB Cloud.
spring.datasource.url=jdbc:${DATABASE_URL}&useSSL=true&enabledTLSProtocols=TLSv1.2,TLSv1.3
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server / Port
# Render provides the port via the PORT environment variable automatically
server.port=${PORT:8080}

# File Upload Settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload-dir=./uploads
spring.web.resources.static-locations=classpath:/static/,file:./uploads/
spring.mvc.static-path-pattern=/uploads/**

# Logging
logging.level.org.hibernate.SQL=ERROR
logging.level.org.springframework.web=INFO
